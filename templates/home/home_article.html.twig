{# templates/home/home_article.html.twig #}

{# Héritage du template de base #}
{% extends 'base.html.twig' %}

{# Contenu spécifique - page 'home' #}
{% block body %}

{# Bannière image #}
<div id="banner-image">
    <figure>
        <img src="{{ asset('img/background-technology.jpg') }}" alt="banner-image-technology">
    </figure>
    <div id="banner-title-animate">
        <h1>Publications</h1>
        <p><span><i class="fas fa-rss fa-2x"></i></span>Dernières news et infos ...</p>
    </div>
</div>

<div class="container">
    <section class="publication_wrap">
        {# Titre de la section 'Publications' #}
        <div class="publication_title">
            <p>Publications</p>
        </div>

        <h1>Derniers articles</h1>
        <div id="first_paragraph">
            <p><em>Découvrez mes publications 'à la une', puisées dans l'actualité du web. Toutes les dernières informations pertinentes qui ont retenu mon attention et qui peuvent, je pense, vous intéresser... N'hésitez-pas à réagir, à partager d'autres contenus et à laisser des commentaires !</em></p>
        </div>
        <div id="second_paragraph">
            <p><span><i class="far fa-smile-wink fa-2x"></i></span>Veille technologique - Langages informatiques les plus plébiscités - Dernières tendances en matière de frameworks - Bonnes pratiques de programmation - Marché de l'emploi - Ressources...</p>
        </div>
        <hr>

        {# Affichage des dernières publications #}
        <article class="publication_container">
            {% for article in articles %}
                <div class="card-deck" id="publication_deck">
                    <div class="card">
                        <div class="image_block">
                            <img src="{{ asset('uploads/images/' ~ article.imageFilename) }}" class="card-img-top" alt="...">
                        </div>
                        <div class="card-body publication_card">
                            <h5 class="card-title publication_title">{{article.title | raw}}</h5>
                            <span class="badge badge-pill badge-light publication_badge">{{article.category.title}}</span>
                            <p class="card-text">{{article.introduction | raw}}</p>
                            <p class="card-text"><small class="text-muted">Article publié le {{article.createdAt | date('d/m/Y')}}</small></p>
                            <div id="publication_link">
                                <a href="{{ path('home_article_view', {'id' : article.id}) }}">Lire la suite</a>
                            </div>

                        </div>
                    </div>
                </div>
            {% endfor %}
        </article>
    </section>
</div>

{% endblock body %}
